function Loading(){// this creates the loading function
    return<p className="rounded-lg bg-white p-6 text-slate-600">Loading...</p>
}//the loading function literally just returns the paragraph text Loading... styled using tailwind css for the text which allows us to just show the loading text followed by a white background

export default Loading //allows this function to be exported and used in other files